from setuptools import setup

setup(name='astrapia',
        version='0.1',
        description='Benchmark for local model-agnostic explainers',
        url='',
        packages=['astrapia'],
        zip_safe=False)
